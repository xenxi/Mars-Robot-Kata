using Amdiaz.MartianRobots;
using Xunit;

namespace Amdiaz.Test.MartianRobots
{
    public class RoverShould
    {
        static int[] ORIGIN = new[] { 0, 0 };

        //[Fact]
        //public void start_at_initial_position()
        //{
        //    var perseverance = new Rover();

        //    Assert.Equal(perseverance.BroadcastPosition(), ORIGIN);
        //}

        //[Fact]
        //public void move_forward()
        //{
        //    var oneStepNorth = new[] { 0, 1 };
        //    var perseverance = new Rover();

        //    perseverance.Move("f");

        //    Assert.Equal(perseverance.BroadcastPosition(), oneStepNorth);
        //}

    }
}